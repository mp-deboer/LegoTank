package tankpack.enums;

public enum Direction
{
	// Ordered by clockwise rotation of the joystick, starting from center to forward
	Center, Forward, ForwardRight, Right, BackwardRight, Backward, BackwardLeft, Left, ForwardLeft
}